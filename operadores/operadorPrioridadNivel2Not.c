/*
Operadores de prioridad nivel 2, Not logic y not a nivel de bit.
(!) not logico
~ para sacar la virgulilla alt + ñ
*/

#include <stdio.h>
#include <stdlib.h>

int main()
{
   int a, b, c;
   a = 12;
   printf("el valor not opuesto al logico ! es : %d",!a);
 
    return 0;
}